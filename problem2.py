import math  # Import the math module for the constant π (pi)

# Collect user input for the radius
radius = float(input("Enter the radius of the circle: "))

# Calculate the area of the circle
area = math.pi * (radius ** 2)

# Print the result
print(area)
